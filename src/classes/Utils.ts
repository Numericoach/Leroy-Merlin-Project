export default class Utils {

	/**
	 * Exemple de fonction locale
	 *
	 * @param first Premier nombre
	 * @param second Deuxième nombre
	 * @returns La somme des deux nombres
	 */
	addition(first: number, second: number): number {
		return first + second
	}
}
